Add to RuntimeDef in ox-core/src/runtime.rs:

- struct RuntimeFailureSignal { name, pattern, retriable (default true), tail_bytes (default 65_536) }
- field: failure_signals: Vec<RuntimeFailureSignal> (#[serde(default)])
- struct CompiledFailureSignal { name, regex: regex::Regex, retriable, tail_bytes }
- impl RuntimeDef { fn compile_failure_signals(&self) -> Result<Vec<CompiledFailureSignal>, regex::Error> }

Add regex crate dep to ox-core. Compilation runs on every config load (initial + SIGHUP reload), not just startup.

Red tests in runtime.rs:
- TOML round-trip with full + default fields
- compile_failure_signals returns Err on invalid regex
- compile_failure_signals returns expected names on valid set